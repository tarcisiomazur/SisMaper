<mah:MetroWindow x:Class="SisMaper.Views.ViewParcela"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:SisMaper.Views"
        xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro" xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Dialog:DialogParticipation.Register="{Binding}"
        mc:Ignorable="d"
        Title="Parcela" Height="750" Width="500">


    <mah:MetroWindow.Resources>
        <Dialog:DialogCoordinator x:Key="dialogCoordinator"/>
        <Style
            TargetType="{x:Type TextBlock}">
            <Setter
                Property="TextWrapping"
                Value="NoWrap" />
            <Setter
                Property="TextTrimming"
                Value="None" />
            <Setter
                Property="FontSize"
                Value="16" />
        </Style>
        <Style
            x:Key="TextBoxStyle1"
            TargetType="{x:Type TextBox}"
            BasedOn="{StaticResource MahApps.Styles.TextBox}">
            <Setter
                Property="FontSize"
                Value="16" />
            <Setter
                Property="Height"
                Value="32" />
        </Style>
        <Style
            x:Key="OptionsButtonStyle"
            TargetType="{x:Type Button}">
            <Setter
                Property="FocusVisualStyle"
                Value="{DynamicResource FocusVisual}" />
            <Setter
                Property="Foreground"
                Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <Setter
                Property="HorizontalContentAlignment"
                Value="Center" />
            <Setter
                Property="VerticalContentAlignment"
                Value="Center" />
            <Setter
                Property="Padding"
                Value="10,4" />
            <Setter
                Property="FontSize"
                Value="14" />
            <Setter
                Property="Height"
                Value="31" />
            <Setter
                Property="Width"
                Value="auto" />

            <Setter
                Property="Template">
                <Setter.Value>
                    <ControlTemplate
                        TargetType="{x:Type Button}">
                        <Border
                            x:Name="border"
                            CornerRadius="3,3,3,3"
                            BorderThickness="1"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="true"
                            Visibility="Visible"
                            BorderBrush="#FF939393">
                            <ContentPresenter
                                x:Name="contentPresenter"
                                Focusable="False"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                Margin="{TemplateBinding Padding}"
                                RecognizesAccessKey="True"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger
                                Property="IsDefaulted"
                                Value="true">
                                <Setter
                                    Property="BorderBrush"
                                    TargetName="border"
                                    Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                            </Trigger>
                            <Trigger
                                Property="IsMouseOver"
                                Value="true">
                                <Setter
                                    Property="BorderThickness"
                                    TargetName="border"
                                    Value="1" />
                            </Trigger>
                            <Trigger
                                Property="IsPressed"
                                Value="true">
                                <Setter
                                    Property="Background"
                                    TargetName="border"
                                    Value="{StaticResource Button.Pressed.Background}" />
                            </Trigger>
                            <Trigger
                                Property="IsEnabled"
                                Value="false">
                                <Setter
                                    Property="Background"
                                    TargetName="border"
                                    Value="{StaticResource Button.Disabled.Background}" />
                                <Setter
                                    Property="BorderBrush"
                                    TargetName="border"
                                    Value="{StaticResource Button.Disabled.Border}" />
                                <Setter
                                    Property="TextElement.Foreground"
                                    TargetName="contentPresenter"
                                    Value="{StaticResource Button.Disabled.Foreground}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </mah:MetroWindow.Resources>


    <Grid IsEnabled="{Binding isParcelaEditable}">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Language="pt-br" Text="Índice" FontSize="22" Margin="20, 20, 0, 0" Width="60" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox  
                    Style="{DynamicResource TextBoxStyle1}" BorderThickness="2"
                    HorizontalAlignment="Left"
                    Margin="10,20,0,0"
                    TextWrapping="Wrap"
                    VerticalAlignment="Top"
                    Width="40" 
                    IsEnabled="false"
                    Text="{Binding Parcela.Indice}"/>

                <TextBlock Language="pt-br" Text="Status" FontSize="22" Margin="60, 20, 0, 0" Width="60" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox  
                    Style="{DynamicResource TextBoxStyle1}" BorderThickness="2"
                    HorizontalAlignment="Left"
                    Margin="10,20,0,0"
                    TextWrapping="Wrap"
                    VerticalAlignment="Top"
                    Width="100" 
                    IsEnabled="false"
                    Text="{Binding Parcela.Status}"/>
                
            </StackPanel>
            <Separator Margin="10,10,10,0"/>

            <StackPanel Orientation="Horizontal" Margin="0,40,0,0">
                <TextBlock Language="pt-br" Text="Valor" FontSize="22" Margin="20, 20, 0, 0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox  
                    Style="{DynamicResource TextBoxStyle1}" BorderThickness="2" Language="pt-br"
                    HorizontalAlignment="Left"
                    Margin="30,20,0,0"
                    TextWrapping="Wrap"
                    VerticalAlignment="Top"
                    Width="200" 
                    Text="{Binding Parcela.Valor}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                <TextBlock Language="pt-br" Text="Data de Vencimento" FontSize="22" Margin="20, 20, 0, 0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <DatePicker  BorderThickness="2"
                    Height="32"
                    FontSize="16"
                    HorizontalAlignment="Left"
                    Margin="30,20,0,0"
                    VerticalAlignment="Top"
                    Width="200" 
                    Focusable="False"
                    SelectedDate="{Binding Parcela.DataVencimento}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                <TextBlock Language="pt-br" Text="Data de Pagamento" FontSize="22" Margin="20, 20, 0, 0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox  Style="{DynamicResource TextBoxStyle1}" BorderThickness="2"
                    Height="32"
                    FontSize="16"
                    HorizontalAlignment="Left"
                    Margin="30,20,0,0"
                    VerticalAlignment="Top"
                    Width="200" 
                    IsEnabled="False"
                    Text="{Binding Parcela.DataPagamento, StringFormat={}{0:dd/MM/yyyy  HH:mm:ss}}"/>
            </StackPanel>

            <Separator Margin="10,10,10,0"/>

            <Separator Margin="20, 60, 0, 0" Width="200" HorizontalAlignment="Left" Background="black"/>
            <TextBlock Language="pt-br" Text="Recebimento" FontSize="28" Margin="20, 0, 0, 0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <Separator Margin="20, 0, 0, 0" Width="200" HorizontalAlignment="Left" Background="black"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                <TextBlock Language="pt-br" Text="Moeda" FontSize="22" Margin="20, 20, 0, 0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox  Style="{DynamicResource TextBoxStyle1}" BorderThickness="2" Language="pt-br"
                    Height="32"
                    FontSize="16"
                    HorizontalAlignment="Left"
                    Margin="30,20,0,0"
                    VerticalAlignment="Top"
                    Width="200" 
                    Text="{Binding ValorMoeda}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                <TextBlock Language="pt-br" Text="Crédito" FontSize="22" Margin="20, 20, 0, 0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox  Style="{DynamicResource TextBoxStyle1}" BorderThickness="2" Language="pt-br"
                    Height="32"
                    FontSize="16"
                    HorizontalAlignment="Left"
                    Margin="30,20,0,0"
                    VerticalAlignment="Top"
                    Width="200" 
                    Text="{Binding ValorCredito}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                <TextBlock Language="pt-br" Text="Débito" FontSize="22" Margin="20, 20, 0, 0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox  Style="{DynamicResource TextBoxStyle1}" BorderThickness="2" Language="pt-br"
                    FontSize="16"
                    HorizontalAlignment="Left"
                    Margin="30,20,0,0"
                    VerticalAlignment="Top"
                    Width="200" 
                    Text="{Binding ValorDebito}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                <TextBlock Language="pt-br" Text="Outro" FontSize="22" Margin="20, 20, 0, 0" Width="200" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBox  Style="{DynamicResource TextBoxStyle1}" BorderThickness="2" Language="pt-br"
                    FontSize="16"
                    HorizontalAlignment="Left"
                    Margin="30,20,0,0"
                    VerticalAlignment="Top"
                    Width="200" 
                    Text="{Binding ValorOutro}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,40,0,0">
                <Button 
                    Style="{DynamicResource OptionsButtonStyle}" 
                    Height="50"
                    FontSize="16"
                    HorizontalAlignment="Left"
                    Margin="20,0,0,0"
                    CommandParameter="{Binding}"
                    Command="{Binding Confirmar}">
                    <Button.Content>
                        <TextBlock>Confirmar<LineBreak/> Recebimento</TextBlock>
                    </Button.Content>

                </Button>

                <Button 
                    Style="{DynamicResource OptionsButtonStyle}" 
                    FontSize="16"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                    Margin="180,0,0,0"
                    Content="Salvar"
                    CommandParameter="{Binding}"
                    Command="{Binding Salvar}"/>

                <Button 
                    Style="{DynamicResource OptionsButtonStyle}" 
                    FontSize="16"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Margin="20,0,0,0"
                    Content="Cancelar"
                    PreviewMouseLeftButtonDown="CancelarButton"/>

            </StackPanel>



        </StackPanel>

    </Grid>
</mah:MetroWindow>
