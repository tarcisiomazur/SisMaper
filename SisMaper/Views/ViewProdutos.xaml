<UserControl 
    x:Class="SisMaper.Views.ViewProdutos"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
    x:Name="userControl" 
    xmlns:views="clr-namespace:SisMaper.Views"
    xmlns:viewModels="clr-namespace:SisMaper.ViewModel"
    xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
    Dialog:DialogParticipation.Register="{Binding}"
    mc:Ignorable="d"
<<<<<<< HEAD
    Height="500" Width="1000"
    
    
    >
=======
    Height="450" Width="800">
>>>>>>> faefa09cc491a18e0c5af519d711049cc15de4d3

    <UserControl.DataContext>
        <viewModels:ProdutosViewModel/>
    </UserControl.DataContext>

    <Grid Background="#FFF6F6F6" >
        <DockPanel>
            <ToolBar DockPanel.Dock="Top" >
                <RibbonGroup Header="Alterar Produto">
                    <StackPanel Orientation="Horizontal">
                        <Button Style="{DynamicResource Button1}" CommandParameter="{Binding}" Command="{Binding NovoProduto}">
                            <StackPanel>
                                <icons:PackIconPixelartIcons Kind="AddBox" HorizontalAlignment="Center" />
                                <TextBlock Style="{DynamicResource Head1}" Text="Novo" />
                            </StackPanel>
                        </Button>
                        <Button Style="{DynamicResource Button1}" CommandParameter="{Binding}" Command="{Binding Editar}">
                            <StackPanel>
                                <icons:PackIconModern Kind="EditBox" HorizontalAlignment="Center" />
                                <TextBlock Style="{DynamicResource Head1}" Text="Editar" />
                            </StackPanel>
                        </Button>
                        <Button Style="{DynamicResource Button1}" CommandParameter="{Binding}" Command="{Binding Deletar}">
                            <StackPanel>
                                <icons:PackIconMaterial Kind="DeleteForever" HorizontalAlignment="Center" />
                                <TextBlock Style="{DynamicResource Head1}" Text="Excluir" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </RibbonGroup>
                <Separator />
                <RibbonGroup Header="Filtrar" Width="332">
                    <Grid Width="468">
                        <TextBox x:Name="FilterTextBox" Margin="7,46,161,0"
                                 mah:TextBoxHelper.Watermark="Buscar produto... (Ctrl + F)"
                                 Background="{DynamicResource MahApps.Brushes.Gray9}"
                                 BorderThickness="0"
                                 Style="{DynamicResource TextBox.Search}"
                                 Text="{Binding FilterText, Delay=300, ElementName=userControl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Height="35" Width="300" VerticalAlignment="Top">
                            <TextBox.ToolTip>
                                <StackPanel>
                                    <TextBlock FontWeight="Bold"
                                               Foreground="{DynamicResource MahApps.Brushes.Accent}"
                                               Text="Filtro Avançado:" />
                                    <TextBlock>
                                        <Run Text="Use these chars to match all substrings: &#x9;" />
                                        <Run Text=" " />
                                        <Run Text=" " />
                                        <Run Text=" " />
                                        <Run FontFamily="Courier New" Text="'&amp;' '+' ',' ';'" />
                                    </TextBlock>
                                    <TextBlock>
                                        <Run Text="Use this chars to match any substring: &#x9;" />
                                        <Run Text=" " />
                                        <Run Text=" " />
                                        <Run Text=" " />
                                        <Run FontFamily="Courier New" Text="'|'" />
                                    </TextBlock>
                                </StackPanel>
                            </TextBox.ToolTip>
                        </TextBox>
                        <CheckBox Margin="234,12,163,0" IsChecked="{x:Null}" Content="Inativos" VerticalAlignment="Top" IsThreeState="True"/>
                        <ComboBox x:Name="FiltrarCategorias" Margin="10,9,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="198" mah:TextBoxHelper.Watermark="Categoria" />

                    </Grid>
                </RibbonGroup>
                <Separator/>
                
                <StackPanel >
                    <Button
                        Height="30" Width="120" Margin="5, 10" Content="Categorias" Background="LightGray" CommandParameter="{Binding}" Command="{Binding AbrirCategorias}">
                    </Button>
                    
                    <Button
                        Height="30" Width="120" Margin="5, 10"  Content="Unidades" Background="LightGray" CommandParameter="{Binding}" Command="{Binding AbrirUnidades}">
                    </Button>
                    
                    
                </StackPanel>

                <Separator/>

                <StackPanel Width="200">
                    <CheckBox Height="20" Width="150" Margin="5, 1" Content="Descrição" HorizontalAlignment="Left" x:Name="DescricaoCheckBox" Click="CheckBoxClick" IsChecked="true"/>
                    <CheckBox Height="20" Width="150" Margin="5, 1" Content="Código De Barras" HorizontalAlignment="Left" x:Name="CodigoDeBarrasCheckBox" Click="CheckBoxClick" IsChecked="true"/>
                    <CheckBox Height="20" Width="150" Margin="5, 1" Content="NCM" HorizontalAlignment="Left" x:Name="NCMCheckBox" Click="CheckBoxClick" IsChecked="true"/>
                    <CheckBox Height="20" Width="150" Margin="5, 1" Content="Categoria" HorizontalAlignment="Left" x:Name="CategoriaCheckBox" Click="CheckBoxClick" IsChecked="true"/>
                    <CheckBox Height="20" Width="150" Margin="5, 1" Content="Unidade" HorizontalAlignment="Left" x:Name="UnidadeCheckBox" Click="CheckBoxClick" />
                    <CheckBox Height="20" Width="150" Margin="5, 1" Content="Preço Custo" HorizontalAlignment="Left" x:Name="PrecoCustoCheckBox" Click="CheckBoxClick" IsChecked="true"/>
                    <CheckBox Height="20" Width="150" Margin="5, 1" Content="Preço Venda" HorizontalAlignment="Left" x:Name="PrecoVendaCheckBox" Click="CheckBoxClick" IsChecked="true"/>
                </StackPanel>


            </ToolBar>
            <Border BorderThickness="3" Margin="5" CornerRadius="5" BorderBrush="#FFC4C4C4">
                <DataGrid Style="{DynamicResource MahApps.Styles.DataGrid}" HorizontalContentAlignment="Stretch" x:Name="ProdutosDataGrid"
                          ColumnWidth="*" ItemsSource="{Binding ProdutosList}" AutoGenerateColumns="False"
                          CanUserAddRows="False" IsReadOnly="True" Margin="0,10,0,24"
<<<<<<< HEAD
                          PreviewMouseLeftButtonDown="Produtos_MouseDown" MouseDoubleClick="Produtos_MouseDoubleClick" SelectionMode="Single"
=======
                          MouseDoubleClick="Produtos_MouseDoubleClick"
                          
>>>>>>> faefa09cc491a18e0c5af519d711049cc15de4d3
                          SelectedItem="{Binding ProdutoSelecionado, Mode=TwoWay}">
                    <DataGrid.Columns>
                        <DataGridTextColumn x:Name="DescricaoDataColumn"
                                Width="60*"
                                Header="Descrição"
                                Binding="{Binding Descricao}"
                                IsReadOnly="True" />
                        <DataGridTextColumn x:Name="CodigoDeBarrasDataColumn"
                                Width="40*"
                                Header="Código de Barras"
                                Binding="{Binding CodigoBarras}"
                                IsReadOnly="True" />
                        <DataGridTextColumn x:Name="NCMDataColumn"
                                Width="30*"
                                Header="NCM"
                                Binding="{Binding NCM}"
                                IsReadOnly="True" />
                        <DataGridTextColumn x:Name="CategoriaDataColumn"
                                Width="30*"
                                Header="Categoria"
                                Binding="{Binding Categoria}"
                                IsReadOnly="True" />
                        <DataGridTextColumn x:Name="UnidadeDataColumn"
                                Width="30*"
                                Header="Unidade"
                                Binding="{Binding Unidade}"
                                IsReadOnly="True" Visibility="Hidden"/>
                        <mah:DataGridNumericUpDownColumn x:Name="PrecoCustoDataColumn"
                                Width="25*"
                                Header="Preço de Custo(R$)"
                                StringFormat="{}{0:N2}"
                                Culture="pt-BR"
                                Binding="{Binding PrecoCusto}"
                                IsReadOnly="true" />
                        <mah:DataGridNumericUpDownColumn x:Name="PrecoVendaDataColumn"
                                Width="25*"
                                Header="Preço de Venda(R$)"
                                StringFormat="{}{0:N2}"
                                Culture="pt-BR"
                                Binding="{Binding PrecoVenda}"
                                IsReadOnly="true" />

                    </DataGrid.Columns>


                </DataGrid>
            </Border>

        </DockPanel>
    </Grid>


</UserControl>